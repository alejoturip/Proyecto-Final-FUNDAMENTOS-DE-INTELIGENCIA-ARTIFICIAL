# Dependencias que SOLO se usan en la laptop, nunca en Render.
# Sirven para entrenar (entrenamiento.py), convertir a TFLite (convertir_tflite.py)
# y evaluar (evaluar.py). Requieren TensorFlow completo.
#
# Instalar (con el venv de Python 3.12 activado):
#   pip install -r requirements.txt              # base de producción
#   pip install -r requirements-desarrollo.txt   # + herramientas de desarrollo

# TensorFlow completo: entrenamiento y conversión del modelo.
# Solo tiene wheels para Python 3.11/3.12/3.13 (NO 3.14).
tensorflow-cpu>=2.18,<2.20

# Evaluación del modelo.
scikit-learn>=1.5      # classification_report y confusion_matrix (precision, recall, F1)
matplotlib>=3.9        # dibuja la matriz de confusión y la guarda como PNG
