# IMPORTANTE: usar Python 3.11, 3.12 o 3.13.
# TensorFlow todavía NO publica versiones para Python 3.14: con 3.14 este
# archivo falla con "No matching distribution found for tensorflow-cpu".
# Comprueba tu versión con:  python --version

# Servidor y API
fastapi>=0.115,<1.0
uvicorn[standard]>=0.34
python-multipart>=0.0.20    # necesario para recibir archivos (multipart/form-data)

# Procesamiento de imágenes
pillow>=11.0
numpy>=1.26,<2.2

# Inteligencia Artificial
# Se usa tensorflow-cpu (no tensorflow): pesa ~200 MB menos porque no trae
# las librerías de GPU/CUDA, que ni la laptop ni Render van a usar.
tensorflow-cpu>=2.18,<2.20
