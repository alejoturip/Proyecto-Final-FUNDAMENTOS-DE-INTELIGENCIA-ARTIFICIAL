# Dependencias de PRODUCCIÓN (las que instala Render).
# NO incluye TensorFlow completo: en el servidor solo se PREDICE, usando el
# modelo en formato TFLite con un runtime liviano (ai-edge-litert). Así el
# servicio entra en los 512 MB de RAM del plan gratuito de Render.
#
# El entrenamiento y la evaluación (que sí necesitan TensorFlow) se hacen en la
# laptop con requirements-desarrollo.txt.
#
# IMPORTANTE: usar Python 3.11 o 3.12 (en Render, variable PYTHON_VERSION=3.12.10).

# Servidor y API
fastapi>=0.115,<1.0
uvicorn[standard]>=0.34
python-multipart>=0.0.20    # necesario para recibir archivos (multipart/form-data)

# Procesamiento de imágenes
pillow>=11.0
numpy>=1.26,<2.2

# Inteligencia Artificial (solo inferencia): runtime liviano de TensorFlow Lite.
ai-edge-litert>=1.0
